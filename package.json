{
  "name": "sharemoney",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile"
  ],
  "scripts": {
    "postinstall": "patch-package",
    "bootstrap": "npm install --workspaces --include-workspace-root",
    "dev:mobile": "npm run start --workspace=mobile",
    "dev:server": "supabase functions serve --env-file .env"
  },
  "devDependencies": {
    "imagemin": "^9.0.1",
    "imagemin-pngquant": "^10.0.0",
    "patch-package": "^8.0.1",
    "postinstall-postinstall": "^2.1.0",
    "sharp": "^0.34.5",
    "supabase": "^2.58.5"
  }
}