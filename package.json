{
  "name": "sharemoney",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile",
    "netlify"
  ],
  "scripts": {
    "bootstrap": "npm install --workspaces --include-workspace-root",
    "dev:mobile": "npm run start --workspace=mobile",
    "dev:server": "npm run dev --workspace=netlify"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "supabase": "^2.58.5"
  }
}
