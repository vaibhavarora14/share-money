# PR #293 walkthrough — people-first empty state (#291)

Captured on Expo web (`localhost:8081` → production API) with `alice@test.com`.

| Shot | File | What it shows |
|------|------|----------------|
| Solo empty | `01_solo_empty_add_people.png` | 1 active member: primary **Add people**, secondary **Add expense anyway**, FAB **Add people** |
| ≥2 empty | `02_multi_empty_add_first_expense.png` | 2 members, no txs: primary **Add first expense**, FAB **Add Expense** |

Also mirrored under `web/public/walkthrough/pr-293/` for static hosting.
