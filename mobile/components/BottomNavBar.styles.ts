import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {},
  bar: {
    borderTopWidth: 1,
  },
  navContent: {
    flexDirection: "row",
    justifyContent: "space-around",
    alignItems: "center",
    paddingVertical: 4,
  },
  navItem: {
    alignItems: "center",
    flex: 1,
  },
  navLabel: {
    marginTop: -8,
    marginBottom: 4,
  },
});
